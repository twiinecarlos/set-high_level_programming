# python-almost_a_circle

This project implements a small object-oriented system with a `Base`
class handling id management and JSON serialization, a `Rectangle`
class with validated attributes, and a `Square` class that inherits
from `Rectangle`. Includes a full unittest suite in `tests/`.
