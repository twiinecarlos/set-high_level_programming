# Python - Network #1

This project contains Python scripts that work with HTTP requests using
`urllib` and other networking tools.

## Tasks

- `0-hbtn_status.py` - Fetches a URL using `urllib` and displays information
  about the response body.
