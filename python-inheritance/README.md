# python-inheritance

This directory covers inheritance in Python: base and derived classes,
method overriding, multiple inheritance, and built-in introspection tools
like isinstance(), issubclass(), and dir().
